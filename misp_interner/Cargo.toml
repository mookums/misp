[package]
name = "misp_interner"
version = "0.1.0"
edition = "2024"

[dependencies]
misp_common = { path = "../misp_common" }
misp_num = { path = "../misp_num" }

heapless.workspace = true
thiserror.workspace = true
